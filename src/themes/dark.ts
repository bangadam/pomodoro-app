import { Theme } from "types/Theme";

const theme: Theme = {
  colors: {
    primary: "#FFCA28",
    secondary: "#FFCA28",
    background: "#212121",
    text: "#ffffff",
  },
};

export default theme;
